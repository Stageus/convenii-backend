const router = require("express").Router();

//productIdx의 리뷰 추가하기
router.post("/product/:productIdx", async (req, res, next) => {
    try {
    } catch (err) {}
});

//productIdx의 모든 리뷰 가져오기
router.get("/product/:productIdx/all", async (req, res, next) => {
    try {
    } catch (err) {}
});

module.exports = router;
